import React from "react";
import { InfocardContainer } from './InfocardElements'

const Landing = () => {
  return (
    <InfocardContainer>
      <div>
        <p>infocard here</p>
      </div>
    </InfocardContainer>
  );
};

export default Landing
